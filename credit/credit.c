#include <cs50.h>
#include <stdio.h>

int main(void)
{
    card = get_long("Number: ");
    card = test_invalid(card;)
    if (card == "INVALID")
    {
        printf("INVALID");

    } else {
        
    }
}

int test_invalid(text)
{

}

#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main(void)
{
    number = get_long("Number: ");
    len = strlen(number);

    if (len != 13 || 15 || 16)
    {
        printf("INVALID");
        return 1;
    }

    for (int i = len; i < 0; i -=2)
    {

    }
}

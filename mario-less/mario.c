#include <stdio.h>
#include <cs50.h>

int main(void)
{
    int value;
    do
    {
        value = get_int("Height: ");
    }
    while(value < 1);

    for (int i = 0; i < value; i++)
    {
        


    }
}
